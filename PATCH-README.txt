ACADEMIC GLOBE PATCH v17

Apply this on top of the current v15 installation.

Changes:
- replaces the broken flat world-map header graphic with a cleaner line-art mountain range behind the top header area
- adds a horizontal mountain-range line drawing behind the footer link area so it appears at the very bottom of the page as a continuous background element
- lowers the globe scene further below the main navigation and keeps the globe scene clipped under the nav
- keeps the globe style but removes country-border emphasis by switching to landmass-only outlines, and makes the globe body opaque enough that the far side does not show through behind the visible hemisphere
- opens the language menu upward so it never falls underneath the navigation
- collapses publications / presentations accordions by default on research, publications, and presentations pages
- removes explanatory presentation paragraphs and keeps only the date / venue / event metadata in presentation panels
- softens accordion styling so items read as transparent rows instead of boxes
- keeps contact form fields translucent
- improves mobile wrapping for the research / teaching section tabs

Files included:
- assets/css/academic-globe-v12.css
- assets/js/academic-globe-v12.js
