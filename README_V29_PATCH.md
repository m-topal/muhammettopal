# v29 Mobile Header and Navigation Fix

Upload/replace:

- `assets/css/style.css`

What it fixes:
- Mobile header no longer becomes too tall.
- Wordmark and main name shrink correctly on phone screens.
- Navigation becomes a single horizontal scroll row instead of wrapping/overlapping.
- Prevents the white floating nav/search-like box from covering page content on mobile.
- Keeps the desktop layout unchanged.
